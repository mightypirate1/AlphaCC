import { PegDirective } from './peg.directive';

describe('PegDirective', () => {
  it('should create an instance', () => {
    const directive = new PegDirective();
    expect(directive).toBeTruthy();
  });
});
